name: Ostad CI CD pipeline

on:
    push:
        branches: 
            - main
    pull_request:
        branches:
            - main

jobs: 
    read_the_file:
        runs-on: ubuntu-latest
        steps: 
            - name: Get clone
              uses: actions/checkout@v2
            - name: Read the txt file
              run: cat test.txt